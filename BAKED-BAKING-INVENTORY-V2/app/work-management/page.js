import WorkManagementClient from './work-management-client';

export const metadata = {
  title: 'BAKED Work Management',
};

export default function WorkManagementPage() {
  return <WorkManagementClient />;
}
