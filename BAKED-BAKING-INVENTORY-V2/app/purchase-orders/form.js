'use client'; import {useSubmit} from '../client-submit';
export default function F({suppliers,products}){const {submit,busy}=useSubmit('/api/purchase-orders');return <form onSubmit={submit}><div className="form-grid"><input name="poNumber" placeholder="PO number" required/><select name="supplierId" required defaultValue=""><option value="" disabled>Supplier</option>{suppliers.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input name="expectedDate" type="date"/><select name="productId" required defaultValue=""><option value="" disabled>Product</option>{products.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select><input name="quantityOrdered" type="number" step="0.01" min="0.01" placeholder="Qty ordered" required/><input name="unitCost" type="number" step="0.01" min="0" defaultValue="0" placeholder="Unit cost"/><input name="notes" placeholder="Notes"/></div><div className="actions" style={{marginTop:12}}><button disabled={busy}>Create Purchase Order</button></div></form>}
