import {Shell,EmptyRow} from '../components'; import {prisma} from '../../lib/prisma';
export const dynamic='force-dynamic';
export default async function Page(){const [products,moves,batches]=await Promise.all([prisma.product.findMany({orderBy:{name:'asc'}}),prisma.stockMovement.findMany({orderBy:{createdAt:'desc'},take:500}),prisma.productionBatch.findMany({orderBy:{createdAt:'desc'},take:100})]);const value=products.reduce((s,p)=>s+Number(p.currentStock)*Number(p.costPrice),0);const waste=moves.filter(m=>m.type==='WASTE').reduce((s,m)=>s+Number(m.quantity),0);return <Shell>
<div className="topbar"><div className="title"><h1>Reports</h1><p>Stock value, wastage and production summary.</p></div></div><div className="grid3"><div className="card"><div className="label">Stock Value</div><div className="metric">R {value.toFixed(2)}</div></div><div className="card"><div className="label">Waste Recorded</div><div className="metric">{waste.toFixed(2)}</div></div><div className="card"><div className="label">Production Batches</div><div className="metric">{batches.length}</div></div></div>
<section className="section"><div className="actions"><a href="/api/export/products"><button className="secondary">Products CSV</button></a><a href="/api/export/movements"><button className="secondary">Movements CSV</button></a></div><div className="table-wrap" style={{marginTop:12}}><table><thead><tr><th>SKU</th><th>Product</th><th>Category</th><th>Stock</th><th>Cost</th><th>Value</th></tr></thead><tbody>{products.length?products.map(p=><tr key={p.id}><td>{p.sku}</td><td>{p.name}</td><td>{p.category}</td><td>{Number(p.currentStock)} {p.unit}</td><td>R {Number(p.costPrice).toFixed(2)}</td><td>R {(Number(p.currentStock)*Number(p.costPrice)).toFixed(2)}</td></tr>):<EmptyRow colSpan={6}/>}</tbody></table></div></section></Shell>}
