'use client'; import {useSubmit} from '../client-submit';
export default function F({products}){const {submit,busy}=useSubmit('/api/recipes');return <form onSubmit={submit}><div className="form-grid"><input name="name" placeholder="Recipe name" required/><select name="outputProductId" required defaultValue=""><option value="" disabled>Finished output product</option>{products.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select><input name="outputQuantity" type="number" step="0.01" min="0.01" defaultValue="1" placeholder="Output quantity"/><select name="inputProductId" required defaultValue=""><option value="" disabled>Input ingredient / packaging</option>{products.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select><input name="inputQuantity" type="number" step="0.01" min="0.01" placeholder="Input quantity" required/><input name="notes" placeholder="Notes"/></div><div className="notice" style={{marginTop:12}}>Create one input line per recipe first. This keeps V2 simple and stable; more ingredients can be added later.</div><div className="actions" style={{marginTop:12}}><button disabled={busy}>Create Recipe</button></div></form>}
