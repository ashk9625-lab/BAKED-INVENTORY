'use client'; import {useSubmit} from '../client-submit';
export default function F({products}){const {submit,busy}=useSubmit('/api/transfers');return <form onSubmit={submit}><div className="form-grid"><input name="transferNumber" placeholder="Transfer number" required/><select name="productId" required defaultValue=""><option value="" disabled>Product</option>{products.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select><input name="quantity" type="number" step="0.01" min="0.01" placeholder="Quantity" required/><input name="fromLocation" placeholder="From location" required/><input name="toLocation" placeholder="To location" required/><input name="note" placeholder="Note"/></div><div className="actions" style={{marginTop:12}}><button disabled={busy}>Record Transfer</button></div></form>}
