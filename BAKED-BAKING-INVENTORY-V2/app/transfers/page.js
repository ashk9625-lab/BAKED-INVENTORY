import {Shell,EmptyRow} from '../components'; import {prisma} from '../../lib/prisma'; import Form from './form';
export const dynamic='force-dynamic';
export default async function Page(){const [products,rows]=await Promise.all([prisma.product.findMany({where:{active:true},orderBy:{name:'asc'}}),prisma.stockTransfer.findMany({include:{product:true},orderBy:{createdAt:'desc'},take:100})]);return <Shell><div className="topbar"><div className="title"><h1>Transfers</h1><p>Track movement between stores, kitchens and storage locations.</p></div></div><div className="card"><Form products={products}/></div><section className="section"><div className="table-wrap"><table><thead><tr><th>Transfer</th><th>Product</th><th>Qty</th><th>From</th><th>To</th><th>Date</th></tr></thead><tbody>{rows.length?rows.map(t=><tr key={t.id}><td>{t.transferNumber}</td><td>{t.product.name}</td><td>{Number(t.quantity)} {t.product.unit}</td><td>{t.fromLocation}</td><td>{t.toLocation}</td><td>{t.createdAt.toLocaleString()}</td></tr>):<EmptyRow colSpan={6}/>}</tbody></table></div></section></Shell>}
