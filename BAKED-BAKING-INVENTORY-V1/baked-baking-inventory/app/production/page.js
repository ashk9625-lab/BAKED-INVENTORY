import {Shell} from '../components';import {prisma} from '../../lib/prisma';
export const dynamic='force-dynamic';
export default async function Production(){const batches=await prisma.productionBatch.findMany({orderBy:{createdAt:'desc'},take:50});return <Shell><div className="topbar"><div className="title"><h1>Production</h1><p>Production batch register for the baking team.</p></div></div><div className="card"><p className="label">VERSION 1</p><h2 style={{marginTop:6}}>Production batch workflow</h2><p>This starter version records stock movements and includes the database model for production batches. The next build can add recipes/BOMs so one batch automatically consumes ingredients and creates finished stock.</p></div><section className="section"><div className="table-wrap"><table><thead><tr><th>Batch</th><th>Product</th><th>Qty Made</th><th>Status</th><th>Date</th></tr></thead><tbody>{batches.length?batches.map(b=><tr key={b.id}><td>{b.batchNumber}</td><td>{b.productName}</td><td>{Number(b.quantityMade)} {b.unit}</td><td>{b.status}</td><td>{b.createdAt.toLocaleString()}</td></tr>):<tr><td colSpan="5">No production batches yet.</td></tr>}</tbody></table></div></section></Shell>}
