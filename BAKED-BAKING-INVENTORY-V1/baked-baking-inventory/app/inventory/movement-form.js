'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function MovementForm({products}){const router=useRouter();const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/stock-movements',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});setBusy(false);if(r.ok){e.currentTarget.reset();router.refresh();}else alert((await r.json()).error||'Could not save movement');}return <form onSubmit={submit}><div className="form-grid"><select name="productId" required defaultValue=""><option value="" disabled>Select product</option>{products.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select><select name="type" defaultValue="RECEIVE"><option>RECEIVE</option><option>ISSUE_TO_PRODUCTION</option><option>PRODUCTION_OUTPUT</option><option>ADJUSTMENT_IN</option><option>ADJUSTMENT_OUT</option><option>WASTE</option></select><input name="quantity" type="number" min="0.01" step="0.01" placeholder="Quantity" required/><input name="batchRef" placeholder="Batch reference (optional)"/><input name="note" placeholder="Reason / note"/></div><div className="actions" style={{marginTop:12}}><button disabled={busy}>{busy?'Saving...':'Save Movement'}</button></div></form>}
